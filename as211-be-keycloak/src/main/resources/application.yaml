# Keycloak Config
#spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8090/realms/PRS


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/
  instance:
    preferIpAddress: true
    hostName: 192.168.43.77

server:
  port: 8087

spring:
  application:
    name: keycloak-service
keycloak:
  realm: PRS
  auth-server-url: http://localhost:8090
  resource: backend